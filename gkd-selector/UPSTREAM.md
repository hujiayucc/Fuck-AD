# Upstream

This module vendors the official GKD selector module from:
https://github.com/gkd-kit/gkd/tree/main/selector

Local project changes:
- `build.gradle.kts` is adapted to build only the JVM target used by the Android app.
- Official selector sources under `src/` are kept in their upstream layout for easier syncing.
